const theme = {
    fontFamily: "'Roboto', 'sans-serif'",
    palette: {
        text: {
            default: '#333'
        }
    }
}

type Theme = typeof theme;

export default theme;
export type { Theme };